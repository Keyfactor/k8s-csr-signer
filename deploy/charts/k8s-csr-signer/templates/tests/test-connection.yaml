apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "command-csr-signer.fullname" . }}-test-connection"
  labels:
    {{- include "command-csr-signer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "command-csr-signer.fullname" . }}:{{ .Values.command.port }}']
  restartPolicy: Never
